/**Local storage key for user payload */
const AUTH_CONST = {
  USER_KEY: "activeUser",
};

/**Local storage key for theme payload */
const THEME = {
  current: "currentTheme",
  dark: "dark-theme",
  light: "light-theme",
};

export { AUTH_CONST, THEME };
